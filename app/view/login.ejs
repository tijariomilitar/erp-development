<html>
    <head>
        <% include ./partials/head %>
    </head>
    <body>
        <header>
            <div onclick="window.location.href='/'" href="#default" class="favicon"><img class="favicon-image" src="/images/favicon/favicon-black.png"></div>
        </header>
        
        <section>
            <div class="container">
                <div class="box one ground padding-10 center">
                    <% if (message.length > 0) { %>
                        <div class="alert-msg"><%= message %></div>
                    <% } %>
                    <form action="/user/login" method="post">
                        <div class="menu-box">
                            <h1 class="title-soft">Faça seu login</h1>
                            <span class="underline"></span>
                            <p>E-mail</p>
                            <input type="email" class="btn-generic-menu-big" name="email" autocomplete="off">
                            <p>Senha</p>
                            <input type="password" class="btn-generic-menu-big" name="password" autocomplete="off">
                            <button type="submit" class="btn-generic-menu-big">Login</button>
                        </div>
                    </form>
                </div>
            </div>
        </section>
    </body>
    <footer>
        <% include ./partials/footer %>
        </script>
    </footer>
</html>